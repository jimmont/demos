'use strict';

describe('product core', function() {
	var $rootScope;
	beforeEach(module('product'));

/*

	directive filterr
	directive activeLink
	routes in config block
		/
toBe ===
.not.toBe(null)
toEqual objects or literals like 12 == 12 {a:1} == {a:1}
toMatch /regex/ or 'string'
toBeDefined
toBeUndefined
toBeNull
toBeTruthy
toBeFalsy
a = [1,2]
expect(a).toContain(1)
toBeLessThan
toBeGreaterThan
toBeCloseTo
toThrow() // any exceptio

*/
/*
	describe('version', function() {
		it('should return current version', inject(function(version) {
			expect(version).toEqual('0.1');
		}));
	});
*/
});
