
an AngularJS with D3 barchart demo

to run this:

prerequisites:
nodejs with npm

cd into this directory then run the following shell commonds:

npm install
npm run start

open in a web browser:
http://localhost:3000

